bool PlayerCanWalkLeft(struct playerModel* model, enum GameState* state){
    return true;
}
bool PlayerCanWalkRight(struct playerModel* model, enum GameState* state){
    return true;
}
bool PlayerCanWalkDown(struct playerModel* model, enum GameState* state){
    return true;
}
bool PlayerCanWalkUp(struct playerModel* model, enum GameState* state){
    return true;
}


void PlayerController(struct playerModel* model, enum GameState* state){
    
}